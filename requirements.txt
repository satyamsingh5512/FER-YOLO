# FER-YOLO-Mamba dependencies
#
# IMPORTANT: do NOT pin torch/torchvision here. Kaggle and RunPod GPU images
# already ship a CUDA build of torch, and reinstalling it usually breaks the
# environment. This file only lists the lightweight pure-Python deps.
#
# The two CUDA extensions (causal-conv1d, mamba-ssm) are NOT listed here on
# purpose. Installing them from PyPI triggers a 20-40 min source compile that
# often fails. Install them with the bundled helper instead, which downloads a
# matching prebuilt wheel for your torch/CUDA/python:
#
#     python install_mamba.py
#
# ---------------------------------------------------------------------------

packaging
einops
timm
tensorboard
scipy
numpy
matplotlib
opencv-python
tqdm
Pillow
h5py
